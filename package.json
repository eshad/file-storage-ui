{
  "name": "storage-server-ui",
  "version": "1.0.0",
  "description": "Professional file storage server with beautiful UI/UX",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "npm install --production && npm run build:assets",
    "build:assets": "echo 'Building production assets...' && npm run minify:css && npm run minify:js",
    "minify:css": "echo 'CSS minification would be done here in a real build process'",
    "minify:js": "echo 'JS minification would be done here in a real build process'",
    "prod": "NODE_ENV=production node server.js",
    "pm2:start": "pm2 start ecosystem.config.js",
    "pm2:stop": "pm2 stop storage-server",
    "pm2:restart": "pm2 restart storage-server",
    "pm2:logs": "pm2 logs storage-server"
  },
  "keywords": ["storage", "file-upload", "file-management", "ui"],
  "author": "Storage Server Team",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "cors": "^2.8.5",
    "path": "^0.12.7",
    "fs-extra": "^11.1.1",
    "uuid": "^9.0.0",
    "compression": "^1.7.4",
    "helmet": "^7.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "pm2": "^5.3.0"
  },
  "engines": {
    "node": ">=14.0.0",
    "npm": ">=6.0.0"
  }
} 